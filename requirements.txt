# DataDealer Dependencies
# Install all of these by running: pip install -r requirements.txt
# Install all of these by running: pip install -r requirements.txt

# --- Web Framework ---
flask==3.0.3                    # The web server and UI framework
flask-apscheduler==1.13.1       # Runs the background email polling job
flask-wtf==1.2.1                # CSRF protection for all forms
flask-limiter==3.8.0            # Rate limiting (brute-force protection on login)

# --- Database ---
# sqlite3 is built into Python — no install needed

# --- Gmail API ---
google-api-python-client==2.131.0   # Core Google API client
google-auth-httplib2==0.2.0         # HTTP transport for Google auth
google-auth-oauthlib==1.2.0         # OAuth 2.0 login flow for Gmail

# --- Anthropic (Claude AI) ---
anthropic>=0.40.0               # Claude API for email parsing

# --- Semantic Search / Embeddings ---
sentence-transformers==3.0.1    # Generates text embeddings for file matching
numpy==1.26.4                   # Math library used for cosine similarity

# --- File Analysis ---
pdfplumber==0.11.0              # Extracts text from PDFs for AI analysis

# --- Production Server ---
gunicorn==22.0.0                # WSGI server for deployment (use instead of Flask dev server)

# --- Utilities ---
python-dotenv==1.0.1            # Loads .env file for secrets
werkzeug==3.0.3                 # Used by Flask for file upload handling
